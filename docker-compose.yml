version: "3.8"

services:
  app:
    container_name: react
    image: react
    command: sh -c "npm rebuild esbuild && yarn dev"
    build:
      context: ./react
      dockerfile: Dockerfile
    volumes:
      - .:/app/react
      # - ./react:/app/react
    ports:
      - 3000:3000
    stdin_open: true
    tty: true
